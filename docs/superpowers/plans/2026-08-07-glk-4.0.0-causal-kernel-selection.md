# GLK 4.0.0 Causal Kernel Selection Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the graph kernel the sole owner of causal path selection, exclusion, typed-seed impact reachability, and source-disposition rules.

**Architecture:** Add pure generic causal functions to `graph_kernel.py`; `graph_model.py` remains a temporary stateful adapter that builds its public dataclasses and applies returned facts. Preserve every existing error condition and mutation boundary.

**Tech Stack:** Python dataclasses, pytest, SHA-256 repository manifest.

---

### Task 1: Ownership RED

**Files:** Modify `tests/test_structure_400.py`.

- [x] Require imports of `select_causal_path`, `causal_impact_slice`, and `validate_source_seed_disposition` from the kernel.
- [x] Forbid the seven superseded private causal methods on `GoGraph`.
- [x] Run the exact structural test and verify RED on current ownership.

### Task 2: Pure Kernel Functions

**Files:** Modify `glk/scripts/graph_kernel.py`, `glk/scripts/graph_model.py`.

- [x] Implement selection/exclusion over generic GO and edge values, returning selected facts and excluded current edges.
- [x] Implement typed-seed forward impact and strict source-disposition validation.
- [x] Translate `GraphKernelError.detail` back to public `GraphError` without changing accepted messages.
- [x] Delete the seven private methods only after focused causal tests pass.

### Task 3: Verification

**Files:** Modify `FILE_HASHES.json`, `tests/test_structure_400.py`.

- [x] Run graph-model and causal-regression tests in normal and optimized modes.
- [x] Enforce `graph_model.py <= 930` and combined model/kernel bounded growth.
- [x] Run the complete suite and repository validator; verify hashes and `git diff --check`.

### Task 4: Record

- [x] Update release-excluded project memory and commit locally.
- [x] Do not push, merge, tag, or release.

## Acceptance

The slice is complete only when causal selection/impact rules have one kernel owner, stateful application behavior is unchanged, all tests pass, and the worktree is clean.
