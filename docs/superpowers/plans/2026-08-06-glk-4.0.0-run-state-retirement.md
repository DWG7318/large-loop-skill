# GLK 4.0.0 Run-State Retirement Plan

**Goal:** Retire the temporary `run_state.py` compatibility truth source after moving its remaining manifest, D1 admission, GO closure, and D1 reuse behavior into the canonical graph kernel.

**Boundary:** Preserve accepted 3.0/3.1 behavior and error codes. Do not change artifacts, authority, common controls, or remote state.

## Task 1: Establish Retirement Gates

- [ ] Require remaining manifest/D1/closure definitions to have one owner in `graph_kernel.py`.
- [ ] Require tests and current repository distribution not to depend on `run_state.py`.
- [ ] Prove RED while the compatibility module still owns the behavior.

## Task 2: Complete the Kernel Migration

- [ ] Move required-CELL manifest, D1 admission, GO closure, and reuse value objects/functions into the kernel.
- [ ] Point CELL-closure, Run-closure, and conformance tests at the kernel replacement reader.
- [ ] Delete `run_state.py` only after direct kernel tests pass.
- [ ] Remove the retired module from repository requirements and hashes.

## Task 3: Verify

- [ ] Run manifest/D1/GO/D3 closure and conformance tests in normal mode.
- [ ] Run the affected security subset under `python -O`.
- [ ] Enforce kernel and total-production budgets.
- [ ] Run the complete repository suite and repository validator.

## Task 4: Record and Commit

- [ ] Regenerate hashes and update any changed validator identity.
- [ ] Record the retirement boundary in release-excluded project memory.
- [ ] Commit locally; do not push, merge, tag, or release.

## Acceptance

The slice passes only when `run_state.py` is absent, all former behavior is kernel-owned, current and historical validation tests pass, production size stays bounded, and the worktree is clean.
