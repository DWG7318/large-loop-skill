# GLK 4.0.0 D3 Kernel Migration Plan

**Goal:** Remove the last production dependency on `run_state.py` by making D3 eligibility and closure digests canonical graph-kernel services.

**Boundary:** Preserve current artifact formats and behavior. This slice does not delete the 3.1 compatibility surface, change common controls, or alter D0–D3 authority.

## Task 1: Establish Ownership Gates

- [ ] Require `run_validation.py` to import D3 facts and closure digest functions from `graph_kernel.py`.
- [ ] Require current production scripts to have no `run_state` import.
- [ ] Require moved definitions to have one executable owner.
- [ ] Prove RED against the current compatibility-owned implementation.

## Task 2: Move Canonical Services

- [ ] Move selected-CELL, current-D2, D3-eligibility value objects to the graph kernel.
- [ ] Move manifest closure, GO candidate digest, and D3 eligibility derivation to the graph kernel.
- [ ] Keep `run_state.py` as a temporary compatibility re-export for existing 3.1 callers and tests.
- [ ] Define the validation-only final Run closure projection in `run_validation.py`.

## Task 3: Verify Behavior and Size

- [ ] Run graph-kernel, CELL-closure, Run-closure, conformance, and formal validator tests.
- [ ] Run affected tests under `python -O`.
- [ ] Add combined kernel/compatibility budgets proving no duplicate growth.
- [ ] Run the complete repository suite and repository validator.

## Task 4: Record and Commit

- [ ] Regenerate `FILE_HASHES.json` and update the validator-bundle digest if required.
- [ ] Record the compatibility boundary in release-excluded project memory.
- [ ] Commit locally; do not push, merge, tag, or release.

## Acceptance

The slice passes only when formal validation has no production import of `run_state.py`, the graph kernel is the sole owner of moved D3/closure rules, compatibility imports still work, all tests pass, and the worktree is clean.
