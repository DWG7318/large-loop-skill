# GLK 4.0.0 Progress Reference Removal Plan

**Goal:** Remove the unused layered-progress reference executor without removing the formal progress contract or its fail-closed validation.

**Boundary:** Delete only `progress_reporting.py` and its direct unit test. Keep `layered-progress.md`, both progress artifact types, schemas, templates, package indexing, authority rules, and Layer 11 validation.

## Task 1: Prove the Duplicate Surface

- [x] Add a structural gate requiring the reference executor and its direct test to be absent.
- [x] Prove RED while both files and both repository requirements still exist.
- [x] Confirm no production module imports the executor.

## Task 2: Remove the Reference Executor

- [x] Delete `glk/scripts/progress_reporting.py` and `tests/test_progress_reporting_310.py`.
- [x] Remove the script from `validate_glk.REQUIRED` and the 3.1 repository surface assertion.
- [x] Keep the formal progress reference, artifact contracts, schemas, templates, and validator logic unchanged.

## Task 3: Prove Formal Coverage Remains

- [x] Map retained tests to missing coverage, duplicate/wrong trigger rejection, scope/version rejection, and ordering rejection.
- [x] Run the Layer 11 progress tests and contract/template tests in normal mode.
- [x] Run the formal progress validation tests under `python -O`.
- [x] Add production/test line budgets that prove a net structural reduction.

## Task 4: Validate and Record

- [x] Regenerate `FILE_HASHES.json`.
- [x] Run repository and structure tests plus `validate_glk.py`.
- [x] Run `git diff --check`, update release-excluded project memory, and commit locally.
- [x] Do not push, merge, tag, or release.

## Acceptance

The slice passes only if the duplicate executable surface is gone while current Layer 11 progress artifacts still validate exactly and fail closed for missing, duplicate, stale, wrong-scope, wrong-trigger, and out-of-order events.
