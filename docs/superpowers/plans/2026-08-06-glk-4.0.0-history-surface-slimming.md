# GLK 4.0.0 History Surface Slimming Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove superseded GLK 2.3.1–3.1.0 construction packets from the current tree while preserving them immutably in Git history.

**Architecture:** Current normative authority already lives in `SKILL.md`, `SPEC.md`, references, schemas, templates, and validators. Historical brainstorming/design/implementation packets are provenance, not runtime authority; delete them from HEAD, retain their commits, and add a repository regression that prevents them from silently returning to the current distribution.

**Tech Stack:** Git history, pytest, GLK repository validator, SHA-256 manifest.

---

### Task 1: Establish Historical-Surface Gate

**Files:**
- Modify: `tests/test_repository_310.py`

- [ ] Add `test_superseded_construction_packets_are_history_only` with the exact six paths listed in Task 2 and assert none exists in the current tree.
- [ ] Run the exact test and verify RED, listing all six present files.

### Task 2: Remove Superseded Packets

**Files:**
- Delete the exact six files listed below.

- [ ] Delete the files with `apply_patch`; do not move or copy them to another live directory.
- [ ] Search the current tree for their full paths and verify no live document depends on them.
- [ ] Run the Task 1 test and verify GREEN.

Exact history-only paths:

```text
docs/superpowers/plans/2026-07-29-glk-2.3.1.md
docs/superpowers/plans/2026-07-30-glk-2.4.0.md
docs/superpowers/plans/2026-08-03-glk-3.0.0-authority-and-run-validation-implementation.md
docs/superpowers/specs/2026-08-03-glk-3.0.0-authority-and-run-validation-design.md
docs/superpowers/plans/2026-08-04-glk-3.1.0-worker-wake-patrol-and-progress-implementation.md
docs/superpowers/specs/2026-08-04-glk-3.1.0-worker-wake-patrol-and-progress-design.md
```

### Task 3: Verify Distribution and Size

**Files:**
- Modify: `FILE_HASHES.json`
- Modify: `tests/test_structure_400.py`

- [ ] Add a current `docs/superpowers` budget of at most 1,250 Markdown lines while the active 4.0 design/plans remain.
- [ ] Prove historical RED from `main` where the same surface exceeds 3,000 lines, then verify current GREEN.
- [ ] Regenerate `FILE_HASHES.json` using the repository helper.
- [ ] Run `tests/test_repository_310.py tests/test_structure_400.py tests/test_templates.py`.
- [ ] Run `python glk/scripts/validate_glk.py .` and verify PASS.
- [ ] Verify `git diff --check` and confirm only planned docs, tests, and hashes changed.

### Task 4: Record and Commit

**Files:**
- Modify canonical checkout `.codex/read-ledger.md` and `.codex/decision-log.md`; both remain release-excluded.

- [ ] Record that pre-4.0 construction packets are Git-history-only and never normative current inputs.
- [ ] Commit current-tree changes as `docs: archive superseded GLK construction packets`.
- [ ] Keep the feature branch/worktree; do not push, merge, tag, release, or delete historical Git commits.

## Acceptance

The slice is complete when exactly six superseded files are absent, active 4.0 design/plan files remain, history-surface budget passes, repository validation passes, and the worktree is clean.
