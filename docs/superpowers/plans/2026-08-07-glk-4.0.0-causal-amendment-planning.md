# GLK 4.0.0 Causal Amendment Planning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the graph kernel the sole producer of validated causal amendment projections while leaving state mutation in `GoGraph`.

**Architecture:** Add immutable kernel facts plus `plan_causal_amendment`; the adapter converts those facts to its public projection and applies them. Preserve error ordering, fail-closed trace revalidation, and append-only history.

**Tech Stack:** Python dataclasses, pytest, SHA-256 repository manifest.

---

### Task 1: Ownership RED

**Files:** Modify `tests/test_structure_400.py`.

- [ ] Require `graph_model.py` to import `plan_causal_amendment`.
- [ ] Forbid amendment planning calls and planning error strings inside `GoGraph.apply_causal_amendment`.
- [ ] Run the exact test and verify RED against the current stateful owner.

### Task 2: Kernel Amendment Plan

**Files:** Modify `glk/scripts/graph_kernel.py`, `glk/scripts/graph_model.py`.

- [ ] Add immutable causal impact facts and a plan result.
- [ ] Revalidate current trace, selected/excluded path, versions, seeds, dispositions, and evidence in the kernel.
- [ ] Derive exact candidate/receipt invalidations and return the plan without mutation.
- [ ] Convert facts to the public projection; keep only state application and history append in `GoGraph`.

### Task 3: Verification

**Files:** Modify `FILE_HASHES.json`, `tests/test_structure_400.py`.

- [ ] Run causal, graph-model, and admission tests in normal and optimized modes.
- [ ] Enforce `graph_model.py <= 835` and no production-line growth.
- [ ] Run the complete suite, repository validator, hashes, and `git diff --check`.

### Task 4: Record

- [ ] Update release-excluded memory and commit locally.
- [ ] Do not push, merge, tag, or release.

## Acceptance

The slice closes only when amendment content has one kernel owner, `GoGraph` only applies the returned plan, all tests pass, and the worktree is clean.
