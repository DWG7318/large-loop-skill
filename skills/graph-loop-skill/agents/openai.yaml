interface:
  display_name: "Graph Loop Skill (GLK)"
  short_description: "Multi-start Node DAGs composed from groups of latest SLK Runs"
  default_prompt: "Use $graph-loop-skill to organize one large Run as a Supervisor-owned Node DAG whose nodes contain groups of latest SLK Runs."
